{
  "name": "runvara-content-command",
  "version": "1.0.0",
  "private": true,
  "description": "RUNVARA trucking content command center for YouTube, Facebook, and Instagram.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check dist/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
